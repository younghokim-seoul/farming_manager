import 'package:flutter/material.dart';

class ItemMenuCard extends StatelessWidget {
  const ItemMenuCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
